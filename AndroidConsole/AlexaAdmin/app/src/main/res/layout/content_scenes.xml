<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="futronix.alexaadmin.activities.MainActivity"
    tools:showIn="@layout/activity_main">

    <Spinner
        android:id="@+id/sceneSpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:entries="@array/scenes_array"
        android:prompt="@string/set_scene_number" />

    <Button
        android:layout_width="80dp"
        android:layout_height="40dp"
        android:textAlignment="center"
        android:text="update"
        android:id="@+id/setSceneButton"
        android:layout_below="@+id/sceneSpinner"
        android:textAllCaps="false"/>

    <include layout="@layout/progress_spinner" />

</RelativeLayout>
